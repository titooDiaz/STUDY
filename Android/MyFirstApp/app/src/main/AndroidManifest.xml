<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/up"
        android:label="@string/app_name"
        android:roundIcon="@drawable/up"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyFirstApp"
        tools:targetApi="31">
        <activity
            android:name=".buscadorDeSuperHeroes.SuperHeroListActivity"
            android:exported="false" />
        <activity
            android:name=".reciclarVistas.ReciclarVistasActivity"
            android:exported="false"
            android:theme="@style/Theme.MySecondTheme" />
        <activity
            android:name=".IMCAPP.ResultActivity"
            android:exported="false" />
        <activity
            android:name=".IMCAPP.IMCActivity"
            android:exported="false" />
        <activity
            android:name=".MenuActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".primeraApp.SegundaActivity"
            android:exported="false" />
        <activity
            android:name=".primeraApp.PrimeraActivity"
            android:exported="true" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>